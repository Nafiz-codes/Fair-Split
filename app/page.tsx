import { redirect } from "next/navigation";
import { createGroup } from "@/app/actions/group";

export default function Home() {
  async function createGroupFromForm(formData: FormData) { "use server"; const group = await createGroup(String(formData.get("name") ?? "")); redirect(`/group/${group.id}`); }
  return <main className="min-h-screen px-5 py-8 sm:px-10 sm:py-12"><header className="mx-auto flex max-w-6xl items-center justify-between border-b pb-5 ledger-rule"><span className="font-editorial text-xl font-semibold tracking-tight">Fair Split</span><span className="font-receipt text-[11px] uppercase tracking-[.18em] text-[var(--slate)]">Travel ledger · est. 2026</span></header><section className="mx-auto grid max-w-6xl gap-10 py-16 lg:grid-cols-[1.2fr_.8fr] lg:py-28"><div className="max-w-2xl"><p className="font-receipt text-xs uppercase tracking-[.2em] text-[var(--brass)]">Shared journeys, clearly settled</p><h1 className="font-editorial mt-5 text-5xl leading-[.98] tracking-tight sm:text-7xl">Keep the trip,<br /><em className="font-normal">not the tally.</em></h1><p className="mt-7 max-w-lg text-base leading-7 text-[var(--slate)]">A quiet ledger for shared travel costs. Add the people, record the receipts, and let the balances resolve themselves.</p><div className="mt-10 flex items-center gap-4 text-sm text-[var(--slate)]"><span className="h-px w-10 bg-[var(--brass)]" />Simple, exact, and ready to settle</div></div><section className="ledger-card self-start rounded-sm p-7 sm:p-9"><p className="font-receipt text-[11px] uppercase tracking-[.18em] text-[var(--slate)]">New travel ledger</p><h2 className="font-editorial mt-3 text-3xl">Start a group</h2><form action={createGroupFromForm} className="mt-8 space-y-6"><label className="block text-sm font-medium" htmlFor="name">Group name<input className="mt-2 w-full border-b border-[var(--border)] bg-transparent px-0 py-3 outline-none placeholder:text-[var(--slate)] focus:border-[var(--brass)]" id="name" name="name" placeholder="Dhaka Trip" required /></label><button className="w-full bg-[var(--ink)] px-4 py-3 text-sm font-semibold text-white transition hover:bg-[var(--brass)]" type="submit">Open a new ledger <span aria-hidden="true">→</span></button></form></section></section><footer className="mx-auto max-w-6xl border-t pt-5 ledger-rule"><p className="font-receipt text-[10px] uppercase tracking-[.16em] text-[var(--slate)]">Receipts, reconciled.</p></footer></main>;
}
